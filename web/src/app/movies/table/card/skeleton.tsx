/**
 * @packageDocumentation
 * @category Component
 */

//#region imports
import { Skeleton } from '@mui/material';
//#endregion

export const CardSkeleton = () => <Skeleton sx={ { borderRadius: 2 } } width={ 182 } height={ 353 } variant='rectangular' />;
